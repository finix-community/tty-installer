[package]
name = "installer-core"
version.workspace = true
edition.workspace = true

[dependencies]
rust-i18n = { version = "4", default-features = false }

[package.metadata.i18n]
available-locales = ["pl", "en-US", "en-GB"]
default-locale = "en-US"
load-path = "locales"
